import { HacerClickDirective } from './hacer-click.directive';

describe('HacerClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HacerClickDirective();
    expect(directive).toBeTruthy();
  });
});
