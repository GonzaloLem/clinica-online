import { CambiarColorEstadoDirective } from './cambiar-color-estado.directive';

describe('CambiarColorEstadoDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarColorEstadoDirective();
    expect(directive).toBeTruthy();
  });
});
