import { FormularioTurnoDirective } from './formulario-turno.directive';

describe('FormularioTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new FormularioTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
