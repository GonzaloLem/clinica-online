import { FormatoDiaPipe } from './formato-dia.pipe';

describe('FormatoDiaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDiaPipe();
    expect(pipe).toBeTruthy();
  });
});
